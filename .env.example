# Production bind
AHAR_HOST=0.0.0.0
AHAR_PORT=8001
AHAR_DB_PATH=data/ahar_tracker.db

# Optional: create accounts on first start (comma-separated user:password pairs)
# AHAR_BOOTSTRAP_USERS=you:yourpassword,partner:theirpassword
